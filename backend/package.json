{
  "name": "expense-dashboard-backend",
  "version": "1.0.0",
  "description": "Express + MongoDB backend for Expense Dashboard (route -> controller -> service -> repository)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.3"
  }
}
